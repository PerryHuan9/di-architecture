{
  "name": "di-architecture",
  "version": "1.3.0",
  "description": "",
  "main": "./dist/index.js",
  "exports": {
    "require": "./dist/index.js",
    "default": "./dist/index.mjs"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "mocha --ui tdd  --require ts-node/register ./test/**/*.ts",
    "build": "tsc",
    "deploy": "npm run build && yarn publish"
  },
  "author": "Perry",
  "license": "ISC",
  "devDependencies": {
    "@types/assert": "^1.5.6",
    "@types/mocha": "^10.0.1",
    "mocha": "^10.2.0",
    "ts-node": "^10.9.1",
    "vite": "^4.5.0",
    "vite-plugin-dts": "^3.6.3"
  }
}
